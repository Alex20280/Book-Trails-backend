export enum BookStatus {
  ToRead = 'toRead',
  Reading = 'reading',
  Read = 'read',
}

export enum Source {
  Purchased = 'purchased',
  Ggifted = 'gifted',
  Borrowed = 'borrowed',
}

export enum BookType {
  Soft = 'soft',
  Audio = 'audio',
  EBook = 'e-book',
}
